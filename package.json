{
  "name": "to_do_pern_stack",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "if-env TYPE=server && (cd server && npm install) || (cd client && npm install)",
    "server": "node ./server/server.js",
    "client": "cd client && cd to-do-app-deployed && npm start"
  },
  "dependencies": {
    "if-env": "^1.0.4"
  }
}
